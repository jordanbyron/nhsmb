#events
  %h1 Fall 2012
  - if events.empty?
    %h2 Coming soon ...
  - events.each do |event|
    .event
      .name
        - if event.url
          = link_to event.name, event.url
        - else
          = event.name
      .score
        = event.score
