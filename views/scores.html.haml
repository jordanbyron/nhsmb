#events
  %h1 Fall 2013
  - if events.empty?
    %h2 Coming soon ...
  - else
    - events.each do |event|
      .event
        .name
          - if event.url
            = link_to event.name, event.url
          - else
            = event.name
        .score
          = event.score
